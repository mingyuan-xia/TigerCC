 LetExp(
  DecList(
   TypeDec(tree,
    RecordTy(
     Fieldlist(
      key
      int,
      true,
      Fieldlist(
       children
       treelist,
       true,
       Fieldlist())))),
   DecList(
    VarDec(d,
     int,
     IntExp(0),
     true),
    DecList(
     TypeDec(treelist,
      RecordTy(
       Fieldlist(
        hd
        tree,
        true,
        Fieldlist(
         tl
         treelist,
         true,
         Fieldlist())))),
     DecList()))),
  SeqExp(
   ExpList(
    IntExp(6))))