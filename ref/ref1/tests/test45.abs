 LetExp(
  DecList(
   TypeDec(rectype,
    RecordTy(
     Fieldlist(
      name
      string,
      true,
      Fieldlist(
       id
       int,
       true,
       Fieldlist())))),
   DecList(
    VarDec(a,
     NilExp(),
     true),
    DecList())),
  SeqExp(
   ExpList(
    varExp(
     SimpleVar(a)))))